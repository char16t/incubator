import { defineMessages } from 'react-intl';

export const scope = 'app.containers.JoinPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the JoinPage container!',
  },
});
