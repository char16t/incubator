import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PasswordResetPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PasswordResetPage container!',
  },
});
