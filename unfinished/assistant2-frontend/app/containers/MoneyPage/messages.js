import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MoneyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MoneyPage container!',
  },
});
